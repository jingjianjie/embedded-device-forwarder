---
session_jsonl: C:\Users\win\.claude\projects\F--013-device-forwarder-embedded-device-forwarder\2c535370-ba3f-4baf-9075-7c32b18ebf41.jsonl
timestamp: 2026-04-28T00:27:14+08:00
status: pending
---

# 阶段 1 C-1: 协议层帧定义 + codec + 规约文档落地

- **任务**: RPD 阶段 1 任务 C-1(子任务):落地新协议层的前半 — 帧定义 (`protocol.h`)、纯函数编解码 (`proto_codec.c`)、协议规约文档 (`doc/router_protocol.md`)、单测 (`tests/unit/test_proto_codec.c`)。registry / dispatcher 留 C-2。

- **产出**(均未提交,留主 agent 决定时机):
  - `routerd/include/protocol.h` — 新建 100 行,定义 `proto_frame_hdr_t`(packed 12 字节)、`proto_cmd_t` 枚举、`PROTO_MAGIC=0xEB7C`、`PROTO_VERSION=1`、`PROTO_MAX_PAYLOAD=64KiB`、5 个错误码、`proto_encode/decode` 签名,`_Static_assert(sizeof(hdr)==12)`
  - `routerd/src/proto_codec.c` — 新建 ~70 行。memcpy 整头方案(规避 packed misalign UB),decode 零拷贝 payload 视图。
  - `doc/router_protocol.md` — 从 1 字节空文件填充到 ~150 行规约:帧布局 ASCII / 字节序声明 / 命令码表 / seq 语义 (R-3) / 错误码表 / 与旧 `ipc_header_t` 不兼容声明 (R-5) / REGISTER 二进制 hex dump 示例 / 待解决项
  - `tests/unit/test_proto_codec.c` — 12 个测试函数,共 27 个 EXPECT 断言。dev x86-64: 27/27 PASS;target aarch64 (RK3588 / OP5+): 27/27 PASS

- **新发现的契约/约束**(候选入库,等 dream):
  - **(C-D) packed wire-format struct 必须通过 memcpy 整头拷贝**,不通过字段指针解引用 — 规避 ARM packed misalign UB。已在 `protocol.h` + `proto_codec.c` 注释固化,后续 dispatcher / SDK 接触 wire-format 时沿用。
  - **(C-E) codec INCOMPLETE 错误码语义不同于真错误**:INCOMPLETE 是流协议正常状态(继续 read),BAD_MAGIC/BAD_VERSION/PAYLOAD_TOO_LARGE 是 fatal(应断连)。dispatcher 在 C-2 调用 codec 时**不能把所有负值统一处理**。
  - **(C-F)** `scripts/deploy_target.py` 的 `put` / `put_dir` 在 Windows + Armbian sshd 组合下仍偶发 ENOENT(即使 PROJECT_CONTEXT.MD 条目 19 的流式写绕开后)。本会话用 `exec` + base64 inline 绕开。考虑把这种 fallback 内化进 deploy_target.py,或在 PROJECT_CONTEXT.MD 条目 19 加补丁说明。

- **未决项**(候选追加 open-questions):
  - **U6** (本会话提出): C-2 registry / dispatcher 子任务是否单独起一个会话?C-2 内容包括 (a) `registry.h/c` 子程序元信息表 (b) `proto_dispatcher.c` 把 cmd 路由到 registry/log_sink/supervisor (c) 用新 codec 替换 `ipc_server.c` 的 `process_frame()` 逻辑(并删旧 `ipc_header_t`)。任务量 ~ C-1 等量或更大。建议主 agent 单独起。
  - **U1**(已存在,本次重申): tests/unit 仍是 test-as-doc 形态。已积累 3 个文件 (`test_plugin_clamp.c` / `test_queue_drop.c` / `test_proto_codec.c`),Unity 脚手架启动时机建议**就在 C-2 起手前**(或与 C-2 同步),避免再增量加 4-5 个 test-as-doc 后一次性重构成本变高。

- **触及的设计意图**:
  - PROJECT_CONTEXT.MD 条目 2 (协议层是地基)、条目 4 (R-5 无前向兼容)、条目 16 (seq 语义钉死)、条目 17 (热加载 non-goal — 协议帧不携带"修改路由"语义)
  - design-intent.md(未读,本次未触及修改)
  - RPD §5.1.1 / §9 R-2 / R-3 / R-5
  - reviews/rpd-v1-2026-04-27.md C-A / C-C(seq 语义)

- **潜在的 PROJECT_CONTEXT.MD 更新建议**(主 agent / 人类决定是否落):
  - 新增条目 [LM_6]: 上述 C-D(packed memcpy 方案)
  - 条目 19 补丁: deploy_target.py 的 ENOENT 在某些 path 仍触发,base64 fallback 是已知绕开方式
