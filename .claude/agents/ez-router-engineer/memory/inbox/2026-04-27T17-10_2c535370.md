---
session_jsonl: C:\Users\win\.claude\projects\F--013-device-forwarder-embedded-device-forwarder\2c535370-ba3f-4baf-9075-7c32b18ebf41.jsonl
timestamp: 2026-04-27T17:10:00
status: pending
---

# RPD v1 批判性 review:10 项问题(4 高 / 5 中 / 1 低)

- **任务**: 主 agent 委派,在 RPD 进入大规模实施前做硬核审查,只产出 review 报告(不改代码、不改文档)。
- **产出**: 10 项 review 条目,主 agent 决定如何更新 `doc/RPD.md`。报告以表格形式输出在对话中。
- **新发现的契约/约束**:
  - **C-A**: `event_queue.c:24-27` 的"双 cond 阻塞 push"在 reactor 单线程消费 + port_send 可能阻塞(慢 TCP 客户端)的场景下,**会反向死锁整个 epoll 循环**。0.6 状态写"已被 5246de2 顺手解决"低估了切面转移。建议加入 design-intent 作为新隐性契约:**reactor 线程不可被任何 send 路径阻塞**,否则 hw watchdog 兜底失效(reactor 还在喂狗)。
  - **C-B**: AF_UNIX SOCK_SEQPACKET 与 splice/sendfile 互斥 — splice 仅在 pipe 一端可用,SEQPACKET 不能直接对接。这是协议传输 + SDK 零拷贝路径的硬冲突,RPD §4 决策表与 §4.1 SDK 描述自相矛盾。
  - **C-C**: 帧头 `seq` 字段语义未定义,是 R5"激进无兼容字段"原则下**最该现在定**的字段,因为后期改 seq 语义比加字段还贵。
- **未决项**:
  - **U3**(候选追加 open-questions): R-1 的解法选哪个 — (a) reactor 旁路喂狗;(b) push 带 timeout drop;(c) 独立 dispatcher 线程只负责 send。各自代价不同,需主 agent / 人类拍板。
  - **U4**(候选): R-2 的解法选哪个 — 改 STREAM 或删 zerocopy 或上 SCM_RIGHTS+shm。影响 SDK API。
  - **U5**(候选): R-5 ez_router 自升级回滚机制 — A/B slot vs 文件备份 + systemd 计数。
- **触及的设计意图**:
  - PROJECT_CONTEXT v3 条目 **2.[LM_1]**(协议是地基) — R-2 / R-3 / R-4 直接质疑当前 RPD 的协议地基设计完整性
  - PROJECT_CONTEXT v3 条目 **5.[LM_3]**(plugin 越界 clamp) + 决议 **R3**(插件崩 → 整板复位)— R-1 揭示 R3 的兜底前提"reactor 还在跑就还在喂狗"在新 push 阻塞策略下不成立;R-6 揭示 R3 缺验收
  - design-intent.md §5 数据流不变量 — R-1 是该不变量的新威胁
  - design-intent.md §9 工作流约束(无前向兼容)— R-3 / R-4 是 R5"激进设计"原则要求**现在**就把语义定准的具体例子
- **dream 时建议追加到 design-intent**:
  - 新增章节"§10 reactor 线程不可阻塞契约":reactor 不得被 send 路径阻塞,否则破坏 R3 watchdog 兜底
- **dream 时建议追加到 change-log**:
  - 2026-04-27 RPD v1 review:产出 10 项问题、主 agent 据此决定 RPD v2 修订范围
